use npc;

include "include/epicai";

var npc := Self();

program KillPlayers()
    Set_Script_Option(SCRIPTOPT_NO_RUNAWAY, 1); //Adicionado para diminuir a quantidade de logs no arquivo script.log

    set_critical(1);
    InitializeEpicNPC(npc);
    set_critical(0);
endprogram