
// :spiders:freeFromWeb
use os;
use uo;

program freeFromWeb(params)
   var mobile := params[1];
   var duration := params[2];
   
   sleep(duration);
   
   if (mobile)
      mobile.frozen := 0;
   endif
endprogram