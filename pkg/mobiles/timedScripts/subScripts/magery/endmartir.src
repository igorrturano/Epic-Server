 
use uo;
use os;

include ":attributes:attributes";

program TimedScript_Clumsy(params)
	var who := params[1];
	var attacker := params[2];
	var level := params[3];
	params := 0; // Not needed anymore.

	
	EraseObjProperty(who, "#martir");
	EraseObjProperty(who, "martirseril");
	
	return 1;
endprogram
   
