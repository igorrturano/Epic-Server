 
use uo;
use os;

include ":attributes:attributes";

program TimedScript_Clumsy(params)
	var who := params[1];
	var attacker := params[2];
	var level := params[3];
	params := 0; // Not needed anymore.

	params := 0; // Not needed anymore.
	
	var amt := cdbl(getObjProperty(who, "#dmgreductionspell"));
	EraseObjProperty(who, "#dmgreductionspell");
	SetObjProperty(who, "dmgreduction", cdbl(GetObjProperty(who, "dmgreduction")) - amt);
	
	return 1;
endprogram
  
