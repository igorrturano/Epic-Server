use uo;
use polsys;

include ":gumps:/include/gumps";
include ":gumps:/include/epicGumps";
include ":gumps:/include/gumps_ex";
include ":destroyable:include/destroyItemPersist";
include ":destroyable:include/destroyItemGump";


program destruiveis(quem, texto)

	var gump := GFECreateGump("Destruiveis",800,600,array{BORDER_BLUE,HIDE_TITLE});
	var arrayLog := ListaLogItemDestruido();
	var y := 70;

	gump := CriaTabelaGump(40,40,800,arrayLog,gump);
	/*foreach log in ( arrayLog )
		GFTextLine(gump, 40, y * _log_iter, 0, "Quando: "+ log.quando);
		GFTextLine(gump, 40,  20 + y * _log_iter, 0, "Onde: " + log.x + " - " + log.y + " - " + log.z + " - " + log.realm);
		GFTextLine(gump, 40, 40 + y * _log_iter, 0, "Quem: "+ log.quem);
		GFTextLine(gump, 40, 60 + y * _log_iter, 0, "O que: "+ log.oque);
		GFTextLine(gump, 40, 80 + y * _log_iter, 0, "Como: "+ log.como);
		GFTextLine(gump, 40, 100 + y * _log_iter, 0, "isReconstruivel: "+ log.isReconstruivel);
	endforeach*/
	var valor := GFSendGump(quem,gump);
endprogram
